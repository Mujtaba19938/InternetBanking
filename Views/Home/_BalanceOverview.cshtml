@model decimal

<div class="bg-gradient-to-r from-blue-600 to-blue-800 rounded-2xl shadow-2xl p-8 mb-8 text-white">
    <div class="flex flex-col items-center text-center">
        <div class="mb-4">
            <h2 class="text-2xl font-light text-blue-100 mb-2">Total Balance</h2>
            <div class="text-5xl font-bold tracking-tight">$@Model.ToString("N2")</div>
        </div>
    </div>
</div>
