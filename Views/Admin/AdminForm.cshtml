@{
    ViewData["Title"] = "Admin Panel";
    Layout = "_AdminLayout";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h3 class="text-center mb-0">🛡️ Admin Panel</h3>
                </div>
                <div class="card-body">
                    <!-- Admin Login Form -->
                    <div class="text-center mb-4">
                        <h4>🔐 Admin Login</h4>
                        <p class="text-muted">Use the default admin credentials below</p>
                    </div>

                    <!-- Default Admin Credentials Display -->
                    <div class="alert alert-info mb-4">
                        <h6 class="mb-2">🔑 Default Admin Credentials:</h6>
                        <div class="row">
                            <div class="col-md-6">
                                <strong>Username:</strong> <code>admin</code>
                            </div>
                            <div class="col-md-6">
                                <strong>Password:</strong> <code>Admin@123</code>
                            </div>
                        </div>
                        <small class="text-muted mt-2 d-block">⚠️ Please change these credentials after first login for security.</small>
                    </div>

                    <!-- Login Form -->
                    <form action="/admin/login" method="post">
                        @Html.AntiForgeryToken()
                        
                        @if (!ViewData.ModelState.IsValid)
                        {
                            <div class="alert alert-danger">
                                <ul class="mb-0">
                                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                                    {
                                        <li>@error.ErrorMessage</li>
                                    }
                                </ul>
                            </div>
                        }

                        <div class="form-group mb-3">
                            <label for="loginUserId" class="form-label">Admin User ID</label>
                            <input name="UserId" id="loginUserId" class="form-control" placeholder="Enter your Admin User ID" value="admin" required />
                        </div>

                        <div class="form-group mb-3">
                            <label for="loginPassword" class="form-label">Password</label>
                            <input name="Password" id="loginPassword" type="password" class="form-control" placeholder="Enter your password" value="Admin@123" required />
                        </div>

                        <div class="form-check mb-3">
                            <input name="RememberMe" id="loginRememberMe" type="checkbox" class="form-check-input" />
                            <label for="loginRememberMe" class="form-check-label">Remember me</label>
                        </div>

                        <button type="submit" class="btn btn-primary w-100">Login as Admin</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
